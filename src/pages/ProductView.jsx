import React from 'react';
import ViewProduct from '../Components/dataTable/ViewProduct';

function ProductView(props) {
    return (
        <div>
            <ViewProduct/>
        </div>
    );
}

export default ProductView;